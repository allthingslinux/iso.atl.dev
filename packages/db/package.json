{
  "name": "@iso/db",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:pull": "drizzle-kit pull",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:check": "drizzle-kit check",
    "db:up": "drizzle-kit up",
    "db:seed": "npx tsx src/run-seed.ts",
    "typecheck": "tsc --noEmit",
    "check": "ultracite check .",
    "fix": "ultracite fix .",
    "format": "ultracite format ."
  },
  "dependencies": {
    "better-auth": "^1.4.9",
    "drizzle-orm": "^0.45.1",
    "drizzle-zod": "^0.6.1",
    "postgres": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "drizzle-kit": "^0.31.8"
  }
}
