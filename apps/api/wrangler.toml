name = "iso-api"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Removed DATABASE_URL - now in .dev.vars for local dev
# In production, set this as a Cloudflare secret

[[kv_namespaces]]
binding = "ISO_CACHE"
id = "xxxx"
preview_id = "xxxx"
